{
  "name": "canvas-free-drawing",
  "version": "1.0.0",
  "description": "Simple and straightforward package and APIs that allows you to draw on a canvas html element.",
  "main": "./lib/canvas-free-drawing.js",
  "scripts": {
    "compile": "babel src -d lib && minify lib/canvas-free-drawing.js -o lib/canvas-free-drawing.min.js",
    "prepublish": "npm run compile"
  },
  "author": "Federico Moretti",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-minify": "^0.4.3",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.4.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ikos9/canvas-free-drawing.git"
  },
  "engines": {
    "node": ">= 8.0.0"
  }
}
